<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>新規会員登録</title>
    <link rel="stylesheet" href="users.css">
    <%= csrf_meta_tags %> <!-- CSRFトークンのメタタグを追加 -->
</head>
<body class="user-page">
    <main class="user-main">
        <h3 class="user-up">新規会員登録</h3>
        <div class="user-container">
            <% @user.errors.full_messages.each do |message| %>
                <div class="form-error">
                    <%= message %>
                </div>
            <% end %>
            <%= form_with url: '/users/create', method: :post, local: true, class: 'registration-form' do |form| %>
                <label for="name">名前</label>
                <%= form.text_field :name, id: 'name', required: true, value: @user.name %>

                <label for="email">メールアドレス</label>
                <%= form.email_field :email, id: 'email', required: true, value: @user.email %>

                <label for="password">パスワード</label>
                <%= form.password_field :password, id: 'password', required: true %>

                <label for="password_confirmation">パスワード確認</label>
                <%= form.password_field :password_confirmation, id: 'password_confirmation', required: true %>

                <button type="submit">登録</button>
            <% end %>
        </div>
    </main>
</body>
</html>
